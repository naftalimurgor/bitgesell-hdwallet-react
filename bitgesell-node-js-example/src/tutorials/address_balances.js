// talk to api
// get balance from endpoint for an address
const axios = require('axios')
const BITGESELL_NODE_RPC = '';
const BITGESELL_EXPLOER_API = 'https://api.bitaps.com/bgl/v1/blockchain'

/**
 * Querying address by making api calls to the the Bitgesell API
 * @param {address} address 
 */
async function getAddressBalance(address) {
    try {
        const headers = {}
        const res = await axios.ge()
    } catch (error) {

    }
}

(() => {

})()